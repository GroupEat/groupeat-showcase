machine:
  node:
    version:
      5.1.0

test:
  override:
    - echo "Nothing to tests..."

deployment:
  production:
    branch: production
    commands:
      - shipit production deploy
  staging:
    branch: staging
    commands:
      - shipit staging deploy
