machine:
  node:
    version:
      5.1.0

test:
  override:
    - echo "Nothing to tests..."

deployment:
  production:
    branch: production
    commands:
      - npm run deploy:production
  staging:
    branch: staging
    commands:
      - npm run deploy:staging
